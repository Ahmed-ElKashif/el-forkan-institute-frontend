/* The level hub's public surface. The level is the product's main destination:
   the six levels the institute teaches, each opening onto its roster,
   catalogue, class days, attendance and scores — with a boys/girls filter for
   the gendered views. Consumed by the route table. */
export { LevelsPage } from './LevelsPage';
export { LevelDetailPage } from './LevelDetailPage';
export { GENDERS, findSection, type Gender } from './level.model';
