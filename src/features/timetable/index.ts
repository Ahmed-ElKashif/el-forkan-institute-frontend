/* The timetable is a tab on the class detail screen, not a destination of its
   own — a class and its weekly grid are the same thing to the head teacher. */
export { SectionTimetableTab } from './SectionTimetableTab';
