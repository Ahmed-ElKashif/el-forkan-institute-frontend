export { TermResultsPage } from './TermResultsPage';
/* The institute-wide term close, and the only way into the screen above: a
   class's term results were previously reachable only by typing the URL. */
export { TermClosePage } from './TermClosePage';
