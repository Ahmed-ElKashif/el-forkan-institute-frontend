/* Academic years & terms — the standard year-setup surface (create year, set
   current, provision the year's classes, edit terms, close). */
export { AcademicYearsPage } from './AcademicYearsPage';
