/* The signed-in user's own account screen — view details, edit what they may,
   and change their password. Reached from the top-bar user menu, not the nav
   rail. */
export { ProfilePage } from './ProfilePage';
