/* Year-end promotion is one institute-wide run, not a tab per level: closing a
   year used to mean six separate runs with nothing showing how far through them
   you were, though the API decides every level in a single call. */
export { PromotionPage } from './PromotionPage';
